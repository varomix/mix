; MIX is indentation-significant, but the current v0 Tree-sitter grammar is
; token-oriented and does not model block structure yet. Keep this query empty
; so Zed loads the language without invalid indentation captures.
